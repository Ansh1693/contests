#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double
int main()
{
    int n, m, x, y;

    cin >> n >> m;
    cin >> x >> y;

    cout << n * x + m * y << endl;

    return 0;
}